{
  "name": "breakout",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install": "yard install",
    "start": "npm run dev",
    "dev": "npx watch 'npm run config' .koji & parcel index.html",
    "build": "npm run config; parcel build index.html",
    "config": "node config.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "parcel-bundler": "^1.12.3",
    "watch": "^1.0.2"
  }
}
